# Анализ проекта Steam AutoReg

## Обнаруженное дублирование кода

### 1. Решение капчи (КРИТИЧНОЕ ДУБЛИРОВАНИЕ)

#### steam_registration.py содержит ДВА метода `solve_captcha()`:

**Первый метод (строки 300-420)** - CapSolver с "разбуживанием":
```python
def solve_captcha(self):
    """Новый метод 2025 года: решает Steam reCAPTCHA v2 Enterprise через CapSolver"""
    # Использует CapSolver API
    # Разбуживает капчу через клик по email
    # Извлекает s-token
    # Отправляет в CapSolver с enterprisePayload
```

**Второй метод (строки 422-656)** - YesCaptcha без "разбуживания":
```python
def solve_captcha(self):
    """Решает hCaptcha на странице Steam с использованием сервиса YesCaptcha или вручную"""
    # Использует YesCaptcha API
    # НЕ разбуживает капчу
    # Ищет hCaptcha iframe
    # Использует устаревший подход
```

**Проблема:** Python использует только ПОСЛЕДНЕЕ определение функции. Первый метод (рабочий CapSolver с разбуживанием) НИКОГДА не вызывается!

**Вердикт:** 🔴 **КРИТИЧЕСКАЯ ОШИБКА** - рабочий код задублирован и не используется

---

### 2. Заполнение email (дублирование в steam_registration.py)

**Метод 1:** `fill_email_address()` (строки 170-216)
**Метод 2:** `confirm_email()` (строки 122-168)
**Метод 3:** `fill_confirm_email()` (строки 218-252)

**Дублирование:**
- Все три метода используют идентичный код для ActionChains
- Одинаковый паттерн: move_to_element → pause → click → send_keys с задержками
- Отличается только ID поля: "email" vs "reenter_email"

**Вердикт:** ⚠️ **Средний приоритет** - можно извлечь в общий метод `_fill_field_with_typing(field_id, text)`

---

### 3. Проверка API ключей (повторяющийся паттерн)

#### Файлы с одинаковой логикой:
1. **check_2captcha_key.py** - проверка 2Captcha API
2. **fix_capsolver_key.py** - очистка CapSolver API ключа
3. **diagnose_azcaptcha.py** - диагностика AZcaptcha

**Дублирование:**
- Загрузка API ключа из файла (try/except FileNotFoundError)
- Настройка кодировки для Windows (строки 12-14 в каждом файле)
- Форматирование вывода API ключа (first 8 chars ... last 4 chars)
- Проверка баланса через GET запрос

**Вердикт:** ⚠️ **Средний приоритет** - можно создать утилиту `src/utils/config_loader.py`

---

### 4. Настройка браузера (повторяющийся код)

#### Идентичная настройка в файлах:
1. **steam_test_stealth.py** - класс `SteamTestStealth`
2. **steam_registration.py** - метод `setup_browser()`
3. **test_2captcha_steam.py** - функция создания Chrome
4. **test_steam_enterprise_solver.py** - функции для Firefox/Chrome

**Дублирование:**
```python
# Повторяется везде:
options.add_argument('--no-sandbox')
options.add_argument('--disable-blink-features=AutomationControlled')
options.add_experimental_option('excludeSwitches', ['enable-automation'])
options.add_experimental_option('useAutomationExtension', False)
driver.execute_script("Object.defineProperty(navigator, 'webdriver', {get: () => false});")
```

**Вердикт:** ⚠️ **Средний приоритет** - можно создать `src/utils/browser_factory.py`

---

### 5. Извлечение Steam sitekey (дублирование концепции)

#### Файлы:
1. **get_steam_sitekey.py** - полноценная утилита для извлечения
2. **diagnose_azcaptcha.py** (строки 105-143) - встроенное извлечение sitekey
3. **test_2captcha_steam.py** - хардкод sitekey

**Дублирование:**
- Открытие страницы Steam в headless режиме
- Поиск iframe с hcaptcha/recaptcha
- Регулярное выражение для парсинга sitekey из URL

**Вердикт:** ✅ **Низкий приоритет** - get_steam_sitekey.py уже выделен в отдельную утилиту

---

### 6. Кодировка консоли Windows (100% дублирование)

**Найдено в КАЖДОМ тестовом скрипте:**
```python
import sys
import io

# Настройка кодировки для Windows консоли
if sys.platform == 'win32':
    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
    sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding='utf-8')
```

**Файлы:**
- check_2captcha_key.py (строки 7-14)
- diagnose_azcaptcha.py (строки 13-19)
- fix_capsolver_key.py (строки 9-14)
- get_steam_sitekey.py (строки 15-17)
- test_2captcha_minimal.py (строки 8-14)
- test_2captcha_task_api.py (строки 14-16)
- test_mobileproxy_api.py (строки 13-15)

**Вердикт:** ✅ **Высокий приоритет** - создать `src/utils/console_utils.py` с функцией `setup_windows_console()`

---

## Структура проекта (текущая)

```
steam_autoreg/
├── steam_registration.py              # Основной скрипт регистрации
├── steam_registration_batch.py        # Пакетная регистрация
├── steam_test_stealth.py              # Тестовый браузер со стелс
├── test_2captcha_steam.py             # Тест CapSolver (РАБОЧИЙ!)
├── test_steam_enterprise_solver.py    # Тест нового модуля
├── test_2captcha_minimal.py           # Минимальный тест 2Captcha
├── test_2captcha_task_api.py          # Тест Task API 2Captcha
├── test_mobileproxy_api.py            # Тест MobileProxy
├── check_2captcha_key.py              # Проверка ключа 2Captcha
├── diagnose_azcaptcha.py              # Диагностика AZcaptcha
├── fix_capsolver_key.py               # Исправление ключа CapSolver
├── get_steam_sitekey.py               # Извлечение sitekey
├── src/
│   ├── captcha/
│   │   ├── azcaptcha_solver.py
│   │   ├── yescaptcha_solver.py
│   │   ├── universal_captcha_solver.py
│   │   └── steam_enterprise_solver.py  # Новый модуль (рабочий!)
│   ├── stealth/
│   │   ├── fingerprint_generator.py
│   │   ├── cookie_generator.py
│   │   ├── storage_generator.py
│   │   ├── human_typing.py
│   │   ├── human_mouse.py
│   │   └── geo_config.py
│   ├── proxy/
│   │   └── mobileproxy_manager.py (?)
│   └── utils/
│       ├── account_queue.py
│       └── storage_generator.py (дубликат?)
├── firefox_antidetect_extension/
└── bin/ (существует, но не используется)
```

---

## План реорганизации

### Цель: Оставить только steam_test_stealth.py в корне

### Структура после реорганизации:

```
steam_autoreg/
├── steam_test_stealth.py              # ✅ ОСТАЁТСЯ (главный тестовый скрипт)
├── steam_registration.py              # ✅ ОСТАЁТСЯ (основная регистрация)
├── steam_registration_batch.py        # ✅ ОСТАЁТСЯ (пакетная регистрация)
│
├── tests/                              # 🆕 Все тесты здесь
│   ├── __init__.py
│   ├── test_2captcha_steam.py         # ← перемещён
│   ├── test_steam_enterprise_solver.py # ← перемещён
│   ├── test_2captcha_minimal.py       # ← перемещён
│   ├── test_2captcha_task_api.py      # ← перемещён
│   └── test_mobileproxy_api.py        # ← перемещён
│
├── bin/                                # 🆕 Утилиты командной строки
│   ├── check_2captcha_key.py          # ← перемещён
│   ├── check_capsolver_key.py         # ← переименован из fix_capsolver_key.py
│   ├── diagnose_azcaptcha.py          # ← перемещён
│   └── get_steam_sitekey.py           # ← перемещён
│
├── src/
│   ├── captcha/
│   │   ├── __init__.py
│   │   ├── azcaptcha_solver.py
│   │   ├── yescaptcha_solver.py
│   │   ├── universal_captcha_solver.py
│   │   └── steam_enterprise_solver.py
│   ├── stealth/
│   │   ├── __init__.py
│   │   ├── fingerprint_generator.py
│   │   ├── cookie_generator.py
│   │   ├── storage_generator.py
│   │   ├── human_typing.py
│   │   ├── human_mouse.py
│   │   └── geo_config.py
│   ├── proxy/
│   │   ├── __init__.py
│   │   └── mobileproxy_manager.py
│   └── utils/
│       ├── __init__.py
│       ├── account_queue.py
│       ├── console_utils.py            # 🆕 Windows console setup
│       ├── browser_factory.py          # 🆕 Browser configuration
│       └── config_loader.py            # 🆕 API key loading
│
├── firefox_antidetect_extension/
├── *.txt                               # Конфиги (остаются в корне)
└── *.md                                # Документация (остаётся в корне)
```

---

## Критические исправления (ПЕРЕД реорганизацией)

### 1. 🔴 СРОЧНО: Исправить steam_registration.py

**Проблема:** Два метода `solve_captcha()` — второй перезаписывает первый!

**Решение:**
```python
# Удалить ВТОРОЙ метод solve_captcha() (строки 422-656)
# Оставить только ПЕРВЫЙ метод (строки 300-420) с CapSolver

# ИЛИ переименовать методы:
def solve_captcha_capsolver(self):  # Первый (рабочий)
def solve_captcha_yescaptcha(self):  # Второй (устаревший)

# И вызывать нужный в зависимости от конфигурации
```

### 2. ⚠️ Использовать steam_enterprise_solver.py

**Файл уже создан и рабочий!** Нужно интегрировать:

```python
# В steam_registration.py заменить solve_captcha():
from src.captcha.steam_enterprise_solver import SteamEnterpriseCaptchaSolver

def solve_captcha(self):
    solver = SteamEnterpriseCaptchaSolver(service='capsolver', debug=True)
    return solver.solve_and_inject(self.driver)
```

---

## Рекомендации по дедупликации

### Создать новые утилиты:

#### 1. src/utils/console_utils.py
```python
def setup_windows_console():
    """Настройка кодировки консоли для Windows"""
    if sys.platform == 'win32':
        sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding='utf-8')
        sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding='utf-8')
```

#### 2. src/utils/browser_factory.py
```python
def create_stealth_firefox(headless=False, proxy=None):
    """Создаёт Firefox с антидетект настройками"""
    # Общая логика настройки Firefox

def create_stealth_chrome(headless=False, proxy=None):
    """Создаёт Chrome с антидетект настройками"""
    # Общая логика настройки Chrome
```

#### 3. src/utils/config_loader.py
```python
def load_api_key(service_name, env_var=None):
    """
    Универсальная загрузка API ключей
    service_name: '2captcha', 'capsolver', 'azcaptcha', etc.
    """
    # Пробует env variable, потом файл {service}_config.txt

def format_api_key(key, show_chars=8):
    """Форматирует API ключ для безопасного вывода"""
    return f"{key[:show_chars]}...{key[-4:]}"
```

#### 4. src/utils/form_filler.py
```python
class HumanFormFiller:
    def fill_field(self, driver, field_id, text):
        """Универсальное заполнение поля с эмуляцией человека"""
        # Используется в fill_email_address, confirm_email, etc.
```

---

## Приоритеты исправлений

### 🔴 Критично (сделать СЕЙЧАС):
1. Исправить дублирование `solve_captcha()` в steam_registration.py
2. Интегрировать `steam_enterprise_solver.py` в основные скрипты

### 🟡 Высокий приоритет:
3. Создать `src/utils/console_utils.py`
4. Переместить тестовые скрипты в `tests/`
5. Переместить диагностические утилиты в `bin/`

### 🟢 Средний приоритет:
6. Создать `src/utils/browser_factory.py`
7. Создать `src/utils/config_loader.py`
8. Создать `src/utils/form_filler.py`

### ⚪ Низкий приоритет:
9. Рефакторинг методов заполнения форм
10. Документация обновлённой структуры

---

## Оценка объёма дублирования

**Всего строк дублированного кода:** ~500-600 строк

**Распределение:**
- Настройка консоли Windows: ~70 строк (7 файлов × 10 строк)
- Настройка браузера: ~150 строк (4 файла × 35 строк)
- Проверка API ключей: ~200 строк (3 файла с похожей логикой)
- Методы solve_captcha: ~350 строк (2 полных метода в одном файле)
- Заполнение форм: ~100 строк (3 почти идентичных метода)

**Потенциал сокращения:** До 40% кода за счёт дедупликации

---

## Выводы

1. ✅ **Проект структурирован хорошо** - разделение на src/captcha, src/stealth, src/proxy
2. ⚠️ **Критическая ошибка** - дублирование метода solve_captcha() в steam_registration.py
3. ⚠️ **Захламление корня** - 12 .py файлов в корне, нужно оставить только 3
4. ✅ **Новый модуль отличный** - steam_enterprise_solver.py решает проблему дублирования
5. 📊 **Много тестовых файлов** - нужна структура tests/ для организации

**Следующий шаг:** Начать реорганизацию с критических исправлений
