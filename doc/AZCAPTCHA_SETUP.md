# AZcaptcha Integration Guide

Руководство по интеграции сервиса AZcaptcha для автоматического решения hCaptcha в проекте Steam Auto Registration.

## Что такое AZcaptcha?

**AZcaptcha** - это дешёвый и надёжный сервис для автоматического решения капч различных типов, включая hCaptcha.

### Преимущества:
- ✅ **Низкая цена**: $1 за 1000 решённых hCaptcha (в 2-3 раза дешевле конкурентов)
- ✅ **Простой API**: GET/POST запросы, JSON ответы
- ✅ **Поддержка прокси**: можно передать прокси для более точного решения
- ✅ **Документация**: https://azcaptcha.com/document

### Недостатки:
- ⚠️ Скорость решения может быть ниже чем у premium сервисов (15-30 секунд)
- ⚠️ Меньше воркеров в non-peak часы

## Шаг 1: Регистрация и получение API ключа

1. Зарегистрируйтесь на https://azcaptcha.com
2. Пополните баланс (минимум $1)
3. Перейдите в раздел **"API Key"**
4. Скопируйте ваш 32-символьный API ключ

## Шаг 2: Настройка проекта

### Создайте конфигурационный файл

```bash
# Скопируйте example файл
cp azcaptcha_config.txt.example azcaptcha_config.txt

# Отредактируйте и вставьте ваш API ключ
nano azcaptcha_config.txt
```

Формат файла `azcaptcha_config.txt`:
```
your_32_character_api_key_here_12345
```

**ВАЖНО**: Не добавляйте `azcaptcha_config.txt` в git! Он уже в `.gitignore`.

### Альтернатива: переменная окружения

```bash
# Linux/Mac
export AZCAPTCHA_API_KEY="your_api_key_here"

# Windows CMD
set AZCAPTCHA_API_KEY=your_api_key_here

# Windows PowerShell
$env:AZCAPTCHA_API_KEY="your_api_key_here"
```

## Шаг 3: Получение актуального sitekey (опционально)

Если вам нужен актуальный sitekey Steam для тестирования:

```bash
# Извлечь sitekey в headless режиме
python get_steam_sitekey.py

# Извлечь sitekey с GUI (чтобы видеть процесс)
python get_steam_sitekey.py --no-headless
```

Утилита автоматически:
- Откроет страницу Steam Join
- Дождётся загрузки hCaptcha
- Извлечёт sitekey из iframe
- Выведет sitekey, URL и дополнительную информацию

## Шаг 4: Тестирование интеграции

Запустите тестовый скрипт для проверки работы API:

```bash
python test_azcaptcha.py
```

Скрипт выполнит:
1. Загрузку API ключа из конфигурации
2. Проверку баланса аккаунта
3. Извлечение актуального sitekey через браузер
4. Создание реальной задачи для решения hCaptcha (списывает $0.001)

**ВНИМАНИЕ**: Тест создаёт реальную задачу и списывает деньги!

## Шаг 5: Использование в проекте

### Standalone использование

```python
from src.captcha.azcaptcha_solver import AZcaptchaSolver, load_azcaptcha_config

# Загружаем API ключ
api_key = load_azcaptcha_config()

# Создаём solver
solver = AZcaptchaSolver(api_key=api_key, debug=True)

# Проверяем баланс
balance = solver.get_balance()
print(f"Balance: ${balance}")

# Решаем капчу
token = solver.solve_hcaptcha(
    website_url="https://store.steampowered.com/join/",
    website_key="your_site_key",
    user_agent="Mozilla/5.0...",  # опционально
    proxy="login:pass@ip:port",   # опционально
    max_attempts=60,               # макс попыток проверки (60 * 5 = 300 сек)
    poll_interval=5                # интервал проверки (рекомендуется 5 сек)
)

if token:
    print(f"✓ Captcha solved! Token: {token}")
else:
    print("✗ Failed to solve captcha")
```

### Через Universal Captcha Solver

```python
from src.captcha.universal_captcha_solver import UniversalCaptchaSolver, CaptchaService

# Создаём универсальный solver с AZcaptcha
solver = UniversalCaptchaSolver(
    service=CaptchaService.AZCAPTCHA,
    api_key="your_api_key",
    debug=True
)

# Решаем капчу
token = solver.solve_hcaptcha(
    website_url="https://store.steampowered.com/join/",
    website_key="your_site_key",
    user_agent="Mozilla/5.0...",
    proxy="login:pass@ip:port",
    max_attempts=60,
    poll_interval=5
)
```

### В скриптах регистрации

Если вы используете `steam_registration.py` или `steam_registration_batch.py`:

1. Создайте файл `azcaptcha_config.txt` с вашим API ключом
2. Модифицируйте скрипт регистрации, чтобы использовать AZcaptcha вместо YesCaptcha

Пример изменения в `steam_registration.py`:

```python
# Вместо YesCaptcha
from src.captcha.azcaptcha_solver import AZcaptchaSolver, load_azcaptcha_config

# В методе solve_captcha():
api_key = load_azcaptcha_config()
if not api_key:
    print("❌ AZcaptcha API key not found!")
    return None

solver = AZcaptchaSolver(api_key=api_key, debug=True)
token = solver.solve_hcaptcha(
    website_url=page_url,
    website_key=sitekey,
    user_agent=self.driver.execute_script("return navigator.userAgent"),
    proxy=self.proxy,  # если используете прокси
    max_attempts=60,
    poll_interval=5
)
```

## API Flow

### 1. Создание задачи (in.php)

```
GET/POST https://azcaptcha.com/in.php
Параметры:
- key: API ключ
- method: hcaptcha
- sitekey: data-sitekey из элемента hCaptcha
- pageurl: URL страницы с капчей
- json: 1 (для JSON ответа)
- userAgent: User-Agent браузера (опционально)
- proxy: login:pass@ip:port (опционально)
- proxytype: HTTP/HTTPS/SOCKS4/SOCKS5 (опционально)

Ответ: {"status":1,"request":"task_id"}
```

### 2. Получение результата (res.php)

```
GET https://azcaptcha.com/res.php
Параметры:
- key: API ключ
- action: get
- id: task_id (из предыдущего запроса)
- json: 1

Ответы:
- В процессе: {"status":0,"request":"CAPCHA_NOT_READY"}
- Решено: {"status":1,"request":"P0_eyJ0eXAiOiJKV1QiLCJ..."}
- Ошибка: {"status":0,"request":"ERROR_CODE"}
```

### 3. Вставка токена на страницу

```javascript
// Токен вставляется в оба поля
document.querySelector('[name="h-captcha-response"]').value = token;
document.querySelector('[name="g-recaptcha-response"]').value = token;

// Затем отправляется форма
document.querySelector('form').submit();
```

## Коды ошибок

| Код ошибки | Описание | Решение |
|------------|----------|---------|
| ERROR_WRONG_USER_KEY | Неверный формат API ключа | Проверьте формат (32 символа) |
| ERROR_KEY_DOES_NOT_EXIST | API ключ не существует | Проверьте правильность ключа |
| ERROR_ZERO_BALANCE | Недостаточно средств | Пополните баланс на сайте |
| ERROR_PAGEURL | Отсутствует pageurl | Добавьте параметр pageurl |
| ERROR_CAPTCHA_UNSOLVABLE | Капча не решена после попыток | Попробуйте ещё раз |
| CAPCHA_NOT_READY | Решение ещё не готово | Подождите и повторите запрос |
| ERROR_BAD_PROXY | Неверный формат прокси | Проверьте формат прокси |
| ERROR_PROXY_CONNECTION_FAILED | Не удалось подключиться к прокси | Проверьте работоспособность прокси |

## Troubleshooting

### Проблема: "API key not found"
**Решение**:
- Проверьте наличие файла `azcaptcha_config.txt`
- Убедитесь что ключ не содержит лишних пробелов/переносов
- Попробуйте использовать переменную окружения `AZCAPTCHA_API_KEY`

### Проблема: "ERROR_ZERO_BALANCE"
**Решение**:
- Проверьте баланс на https://azcaptcha.com
- Пополните баланс (минимум $1)
- Стоимость: $1 = 1000 решённых капч

### Проблема: "CAPCHA_NOT_READY" долгое время
**Решение**:
- Увеличьте `max_attempts` (по умолчанию 60)
- Увеличьте `poll_interval` до 10 секунд
- Проверьте нагрузку на сервис (возможны задержки в non-peak часы)

### Проблема: "ERROR_INVALID_SITEKEY"
**Решение**:
- Sitekey устарел или неверен
- Извлеките актуальный sitekey со страницы:
  ```bash
  python get_steam_sitekey.py
  ```
- Sitekey может меняться со временем, используйте актуальный
- Убедитесь что используете правильный URL страницы

### Проблема: "ERROR_CAPTCHA_UNSOLVABLE"
**Решение**:
- Проверьте правильность `sitekey` и `pageurl`
- Убедитесь что капча видима на странице
- Попробуйте передать User-Agent браузера
- Попробуйте использовать прокси того же региона что и целевой сайт

### Проблема: Низкая скорость решения
**Решение**:
- Используйте прокси того же региона что и сайт
- Передавайте User-Agent браузера
- Попробуйте решать в peak hours (10:00-22:00 UTC)
- Рассмотрите возможность использования premium сервисов (2Captcha, CapSolver)

## Сравнение с другими сервисами

| Сервис | Цена за 1000 капч | Скорость | Качество | Рекомендация |
|--------|------------------|----------|----------|--------------|
| AZcaptcha | $1 | 15-30 сек | Хорошее | ✅ Budget вариант |
| 2Captcha | $2-3 | 10-20 сек | Отличное | ✅ Для продакшена |
| CapSolver | $2.5-4 | 10-15 сек | Отличное | ✅ Для сложных капч |
| AntiCaptcha | $2-3 | 15-25 сек | Хорошее | ⚠️ Альтернатива |
| YesCaptcha | $1.5-2 | 20-30 сек | Хорошее | ⚠️ Backup вариант |

## Полезные ссылки

- 🌐 Официальный сайт: https://azcaptcha.com
- 📖 Документация API: https://azcaptcha.com/document
- 💰 Пополнение баланса: https://azcaptcha.com/deposit
- 📊 История операций: https://azcaptcha.com/history
- 🔑 API ключи: https://azcaptcha.com/settings

## Безопасность

⚠️ **ВАЖНО**:
- Никогда не коммитьте файл `azcaptcha_config.txt` в git
- Не делитесь вашим API ключом
- Регулярно проверяйте баланс и историю операций
- Используйте разные API ключи для разных проектов

## Лицензия

Этот код предоставляется "как есть" для образовательных целей. Использование сервисов решения капч может нарушать условия использования целевых сайтов. Используйте на свой риск.

---

**Последнее обновление**: Декабрь 2025
**Автор интеграции**: Claude Code Assistant