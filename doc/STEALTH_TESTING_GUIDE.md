# Руководство по тестированию Stealth модулей

## Введение

Этот набор тестов позволяет полностью проверить качество fingerprint обходов в вашем проекте Outlook. Все тесты используют реальный `BrowserManager` со всеми stealth модулями.

## Доступные тесты

### 1. 🚀 quick_stealth_check.py - Быстрая локальная проверка
**Время выполнения:** 5-10 секунд
**Нужен интернет:** Нет
**Лучше всего для:** Первичная проверка, отладка

```bash
python quick_stealth_check.py
```

**Что проверяет:**
- ✓ navigator.webdriver (КРИТИЧНО!)
- ✓ Automation маркеры
- ✓ Window dimensions (headless detection)
- ✓ WebGL vendor/renderer
- ✓ Hardware (cores, memory)
- ✓ Web APIs (storage, connection, contacts)
- ✓ Notification.permission
- ✓ Speech Synthesis voices
- ✓ Plugins & MimeTypes

**Преимущества:**
- Мгновенный результат
- Не требует интернета
- Идеально для отладки
- Показывает все критичные параметры

**Когда использовать:**
- Перед запуском полных тестов
- При изменении fingerprint_generator.py
- Для быстрой проверки webdriver override
- При отладке headless режима

---

### 2. 🎯 creepjs_test.py - Детальный тест CreepJS
**Время выполнения:** 2-3 минуты
**Нужен интернет:** Да
**Лучше всего для:** Полная проверка fingerprint обходов

```bash
python creepjs_test.py
```

**Что проверяет:**
- ✓ Все что есть в quick_check
- ✓ Canvas fingerprinting
- ✓ Audio fingerprinting
- ✓ Font fingerprinting
- ✓ Screen fingerprinting
- ✓ Timezone consistency
- ✓ WebRTC leak
- ✓ ~50+ других тестов CreepJS

**Что делает:**
1. Открывает CreepJS (abrahamjuliot.github.io/creepjs)
2. Ждет завершения всех тестов (до 100%)
3. Анализирует результаты и извлекает "lies"
4. Проверяет конкретные значения
5. Делает скриншот
6. Оставляет браузер открытым для ручной проверки

**Преимущества:**
- Самый детальный анализ
- Показывает все "лжи" (lies)
- Скриншот результатов
- Используется реальными антифрод системами

**Когда использовать:**
- Финальная проверка перед продакшеном
- При изменении Canvas/Audio/WebGL кода
- Для сравнения разных fingerprint профилей
- Чтобы понять какие конкретно тесты проваливаются

---

### 3. 🌐 multi_platform_stealth_test.py - Мультиплатформенный тест
**Время выполнения:** 4-6 минут
**Нужен интернет:** Да
**Лучше всего для:** Полная проверка на разных платформах

```bash
python multi_platform_stealth_test.py
```

**Что проверяет:**
1. **IPHey** - IP, country, ISP, headers
2. **BrowserLeaks (WebRTC)** - Утечка локального IP
3. **Pixelscan** - Trust Score, визуальный анализ
4. **CreepJS** - Продвинутая детекция

**Что делает:**
1. Последовательно посещает 4 платформы
2. На каждой делает проверку и скриншот
3. Выводит итоговый отчет с оценкой
4. Показывает статистику (PASS/WARN/FAIL)

**Преимущества:**
- Комплексная проверка
- Скриншоты со всех платформ
- Итоговая оценка
- Обнаруживает WebRTC утечки

**Когда использовать:**
- Перед релизом
- Проверка работы прокси
- Комплексная оценка stealth
- Для сравнения с другими anti-detect браузерами

---

## Рекомендуемая последовательность

### Сценарий 1: Быстрая проверка (5 минут)
```bash
# 1. Быстрая локальная проверка
python quick_stealth_check.py

# Если всё OK:
# 2. Полный тест CreepJS
python creepjs_test.py
```

### Сценарий 2: Полная проверка (10 минут)
```bash
# 1. Быстрая локальная проверка
python quick_stealth_check.py

# Если всё OK:
# 2. Мультиплатформенный тест
python multi_platform_stealth_test.py
```

### Сценарий 3: Отладка (1 минута)
```bash
# Только быстрая проверка для отладки
python quick_stealth_check.py
```

---

## Интерпретация результатов

### ✓ Отлично (Trust Score > 90%)
```
navigator.webdriver: false ✓
Automation markers: None ✓
Window dimensions: 1536x864 ✓
Проблем не обнаружено!
```

**Что это значит:**
- Браузер выглядит как обычный пользовательский
- Детекция маловероятна
- Можно использовать в продакшене

**Действия:**
- Ничего не требуется
- Сохраните fingerprint профиль
- Используйте разные профили для разных аккаунтов

---

### ⚠ Хорошо (Trust Score 70-90%)
```
navigator.webdriver: false ✓
WARNING: No speech synthesis voices
WARNING: connection.downlinkMax !== Infinity
Проблем: 2 (предупреждения)
```

**Что это значит:**
- Основные параметры в порядке
- Есть мелкие несоответствия
- Детекция возможна, но маловероятна

**Действия:**
- Проверьте предупреждения
- Исправьте если возможно
- Или продолжайте использовать (риск низкий)

---

### ✗ Плохо (Trust Score < 70%)
```
navigator.webdriver: true ✗
CRITICAL: Zero window dimensions
CRITICAL: Automation markers found
Проблем: 5+ (критичные)
```

**Что это значит:**
- Браузер явно определяется как автоматизированный
- Детекция гарантирована
- Использование в продакшене приведет к блокировке

**Действия:**
1. Проверьте, что `headless=False`
2. Убедитесь, что fingerprint инжектируется ДО загрузки страницы
3. Проверьте `setInterval(overrideWebdriver, 5)` в fingerprint_generator.py
4. Перезапустите тест после исправлений

---

## Критичные параметры

### 1. navigator.webdriver
**Важность:** 🔴 КРИТИЧНО
**Должен быть:** `false`
**НЕ должен быть:** `true`, `undefined`

**Как проверить:**
```javascript
console.log(navigator.webdriver); // должно быть: false
```

**Если проблема:**
- Проверьте fingerprint_generator.py:512-536
- Убедитесь, что `setInterval(overrideWebdriver, 5)` работает
- Проверьте, что инжект через `add_init_script` выполняется ПЕРВЫМ

---

### 2. Window Dimensions
**Важность:** 🔴 КРИТИЧНО
**outerWidth/outerHeight:** Должны быть > 0
**В headless режиме:** Всегда = 0 (детекция!)

**Как проверить:**
```javascript
console.log(window.outerWidth, window.outerHeight);
```

**Если проблема:**
- Убедитесь, что `headless=False`
- Проверьте Firefox anti-headless патчи (fingerprint_generator.py:706-727)

---

### 3. WebRTC Leak
**Важность:** 🟠 ВАЖНО
**Проверка:** browserleaks.com/webrtc

**Если утечка:**
- Локальный IP (192.168.x.x, 10.x.x.x) НЕ должен светиться
- Проверьте WebRTC protection (fingerprint_generator.py:614-661)

---

### 4. Canvas/Audio Tampering
**Важность:** 🟡 СРЕДНЕ
**Проблема:** "Suspected tampering" в CreepJS

**Если проблема:**
- Уменьшите canvas_noise (сейчас: 1-10)
- Проверьте, что изменения применяются только к каждому 40-му пикселю
- Audio должен округляться до 3 знаков (fingerprint_generator.py:474)

---

## Частые проблемы и решения

### Проблема: navigator.webdriver = true
**Причина:** Playwright не успевает скрыть флаг
**Решение:**
1. Убедитесь, что fingerprint инжектируется через `add_init_script`
2. Проверьте, что `setInterval(overrideWebdriver, 5)` работает
3. Перезапустите браузер

### Проблема: Headless detected (outerWidth = 0)
**Причина:** Браузер запущен в headless режиме
**Решение:**
1. Установите `headless=False` в BrowserManager
2. Проверьте Firefox anti-headless патчи

### Проблема: WebRTC leak (local IP visible)
**Причина:** Реальный IP утекает через WebRTC
**Решение:**
1. Проверьте WebRTC protection код
2. Тестируйте на browserleaks.com/webrtc
3. Убедитесь, что proxy настроен правильно

### Проблема: Canvas tampering detected
**Причина:** Слишком грубые изменения canvas
**Решение:**
1. Уменьшите canvas_noise
2. Применяйте изменения реже (каждый 40-й пиксель)

### Проблема: No speech synthesis voices
**Причина:** В headless отсутствуют голоса
**Решение:**
1. Проверьте Firefox speech synthesis патч (fingerprint_generator.py:823-858)
2. Убедитесь, что headless=False

---

## Технические детали

### Как работает fingerprint injection?
1. **До запуска браузера:**
   - Генерируется уникальный fingerprint профиль
   - Определяется геолокация по proxy IP

2. **При запуске браузера:**
   - Инжектируется JavaScript через `add_init_script`
   - Скрипт выполняется ДО загрузки любой страницы

3. **Что делает скрипт:**
   - Переопределяет WebGL vendor/renderer
   - Добавляет Canvas noise
   - Округляет Audio fingerprint
   - Скрывает navigator.webdriver
   - Добавляет недостающие Web APIs
   - Фиксирует window dimensions (для Firefox)

4. **Непрерывная защита:**
   - `setInterval(overrideWebdriver, 5)` каждые 5ms
   - Переопределение на всех событиях (load, focus и т.д.)

---

## Файлы для изучения

### Основной stealth код:
- `src/stealth/fingerprint_generator.py` - Генератор fingerprints
- `outlook/browser.py` - BrowserManager с инжектом

### Критичные секции fingerprint_generator.py:
- **Строки 385-408:** WebGL vendor/renderer
- **Строки 302-380:** Canvas noise
- **Строки 464-497:** Audio rounding
- **Строки 500-608:** navigator.webdriver override
- **Строки 610-661:** WebRTC protection
- **Строки 180-265:** Web APIs (storage, connection, contacts)
- **Строки 706-882:** Firefox anti-headless патчи

---

## Дополнительные ресурсы

### Сайты для тестирования:
- https://abrahamjuliot.github.io/creepjs/ - Самый продвинутый детектор
- https://pixelscan.net/ - Визуальный анализ, Trust Score
- https://browserleaks.com/webrtc - WebRTC leak test
- https://iphey.com/ - Базовая проверка IP/headers

### Полезные ссылки:
- Документация Playwright: https://playwright.dev/
- CreepJS GitHub: https://github.com/abrahamjuliot/creepjs
- Fingerprinting research: https://fingerprintjs.com/

---

## Заключение

Используйте эти тесты регулярно:
- **quick_stealth_check.py** - при каждом изменении кода
- **creepjs_test.py** - перед коммитом
- **multi_platform_stealth_test.py** - перед релизом

Все тесты используют реальный BrowserManager, поэтому результаты точно отражают поведение в продакшене.

**Целевая оценка:** Trust Score > 85%, 0 критичных проблем.

---

**Автор:** Outlook Account Creator Team
**Последнее обновление:** 2025-12-16
